import {z} from 'zod';
export const checkoutSchema=z.object({name:z.string().trim().min(2).max(100),lastName:z.string().trim().min(2).max(100),phone:z.string().regex(/^\+380\d{9}$/),email:z.email(),city:z.string().trim().min(2).max(150),address:z.string().trim().min(2).max(250),cityRef:z.string().max(50).optional(),warehouseRef:z.string().max(50).optional(),delivery:z.enum(['np','locker','ukr','courier']),payment:z.enum(['cod','mono','liqpay','bank']),comment:z.string().max(1000).optional(),promo:z.string().max(40).optional(),consent:z.literal(true),items:z.array(z.object({id:z.string().max(80),quantity:z.number().int().min(1).max(100)})).min(1).max(100),requestId:z.uuid()});
export const productSchema=z.object({id:z.string().min(1).max(80),slug:z.string().regex(/^[a-z0-9-]+$/),name:z.string().min(2).max(160),category:z.string().min(1),price:z.coerce.number().positive().max(1000000),old_price:z.coerce.number().nonnegative().nullable().optional(),weight:z.string().max(40),stock:z.coerce.number().int().nonnegative().max(100000),tags:z.array(z.string().max(30)).max(10),image:z.string().refine(v=>v.startsWith('/images/')||/^https:\/\/[a-z0-9-]+\.supabase\.co\//.test(v),'Використайте завантажене фото'),description:z.string().max(10000),ingredients:z.string().max(4000),nutrition:z.object({kcal:z.coerce.number().nonnegative(),protein:z.coerce.number().nonnegative(),fat:z.coerce.number().nonnegative(),carbs:z.coerce.number().nonnegative()}),featured:z.boolean(),active:z.boolean().optional()});
