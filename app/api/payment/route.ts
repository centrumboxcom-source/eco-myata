import {NextResponse} from 'next/server';
import {createHash} from 'node:crypto';
import {serviceClient} from '@/lib/server';
import {z} from 'zod';
export async function POST(request:Request){const v=z.object({orderId:z.uuid(),requestId:z.uuid()}).safeParse(await request.json().catch(()=>null));const client=serviceClient();if(!v.success||!client)return NextResponse.json({message:'Оплату не налаштовано.'},{status:400});const {data:o}=await client.from('orders').select('*').eq('id',v.data.orderId).eq('request_id',v.data.requestId).single();if(!o||o.status==='Скасовано'||o.payment_status==='paid')return NextResponse.json({message:'Замовлення недоступне для оплати.'},{status:400});const origin=process.env.NEXT_PUBLIC_SITE_URL;if(!origin?.startsWith('https://'))return NextResponse.json({message:'Онлайн-оплата потребує опублікованого HTTPS-сайту.'},{status:503});try{if(o.payment==='mono'&&process.env.MONOBANK_TOKEN){if(o.payment_url)return NextResponse.json({url:o.payment_url});const r=await fetch('https://api.monobank.ua/api/merchant/invoice/create',{method:'POST',headers:{'X-Token':process.env.MONOBANK_TOKEN,'Content-Type':'application/json'},body:JSON.stringify({amount:Math.round(Number(o.total)*100),ccy:980,merchantPaymInfo:{reference:o.id,destination:'Замовлення ЕКО М’ЯТА №'+o.id.slice(0,8)},redirectUrl:origin+'/payment/result',webHookUrl:origin+'/api/webhooks/mono',validity:86400,paymentType:'debit'}),signal:AbortSignal.timeout(15000)});const d=await r.json();if(!r.ok||!d.pageUrl||!d.invoiceId)throw new Error();const {error}=await client.from('orders').update({invoice_id:d.invoiceId,payment_url:d.pageUrl}).eq('id',o.id);if(error)throw error;return NextResponse.json({url:d.pageUrl})}if(o.payment==='liqpay'&&process.env.LIQPAY_PUBLIC_KEY&&process.env.LIQPAY_PRIVATE_KEY){const data=Buffer.from(JSON.stringify({version:7,public_key:process.env.LIQPAY_PUBLIC_KEY,action:'pay',amount:Number(o.total),currency:'UAH',description:'Замовлення ЕКО М’ЯТА №'+o.id.slice(0,8),order_id:o.id,result_url:origin+'/payment/result',server_url:origin+'/api/webhooks/liqpay',language:'uk'})).toString('base64');const signature=createHash('sha3-256').update(process.env.LIQPAY_PRIVATE_KEY+data+process.env.LIQPAY_PRIVATE_KEY).digest('base64');return NextResponse.json({provider:'liqpay',data,signature})}return NextResponse.json({message:'Оберіть доступний спосіб оплати.'},{status:400})}catch{return NextResponse.json({message:'Банк тимчасово не відповідає. Замовлення збережено, повторіть оплату пізніше.'},{status:502})}}
