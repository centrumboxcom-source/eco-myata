'use client';
import {useEffect} from 'react';
import {useShop} from '@/lib/store';
type Tool={name:string;description:string;inputSchema:object;annotations:{readOnlyHint:boolean};execute:(input:unknown)=>unknown};
export default function ShopTools(){useEffect(()=>{const context=(document as Document&{modelContext?:{registerTool:(tool:Tool,options:{signal:AbortSignal})=>void}}).modelContext;if(!context)return;const lifecycle=new AbortController();const tools:Tool[]=[{name:'read_cart',description:'Показати товари, кількість і попередню суму кошика.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:()=>{const {items}=useShop.getState();return {items:items.map(i=>({id:i.product.id,name:i.product.name,quantity:i.quantity,price:i.product.price})),subtotal:items.reduce((s,i)=>s+i.quantity*i.product.price,0),currency:'UAH'}}},{name:'open_cart',description:'Відкрити кошик для перегляду. Не створює замовлення та не здійснює оплату.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:false},execute:(input)=>{if(!input||typeof input!=='object'||Object.keys(input).length)throw new Error('Очікується порожній об’єкт');useShop.getState().setCartOpen(true);return {opened:true}}}];for(const tool of tools){try{void Promise.resolve(context.registerTool(tool,{signal:lifecycle.signal})).catch(()=>{})}catch{}}return()=>lifecycle.abort()},[]);return null}
